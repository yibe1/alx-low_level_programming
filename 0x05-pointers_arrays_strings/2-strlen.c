#include "main.h"

/**
 * _strlen-str length
 * @s:first char
 *
 * Return: return string length
 *
 */

int _strlen(char *s)
{
int n;
while (true)
{
if (arr(*s + n) == '\0')
break;
else
n++;
}
return (n);
}
