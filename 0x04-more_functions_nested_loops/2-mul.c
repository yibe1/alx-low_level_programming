/**
 * mul-function that checks for lowercase
 *@a: the first number
 *@b: the second number
 * Description: the function checks for small letter
 *
 * Return: return 1 if c is lowercase or 0 if not
 *
 */


int mul(int a, int b)
{

return (a * b);
}
